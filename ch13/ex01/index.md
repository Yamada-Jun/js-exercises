### 予想
1秒後に"Hello world!"と表示される。

### 結果
何も表示されず、プログラムが終了しない。

### 理由
setTimeoutにより、タスクキューに、1秒後に実行されるコールバック関数が登録される。
次に、グローバルコンテキストでwhileによる無限ループが実行され、メインスレッドが占有される。
グローバルコンテキストが終了したら、1秒後に"Hello world!"と表示されるはずだが、無限ループが終了しないため、"Hello world!"は表示されず、プログラムも終了しない状態となる。