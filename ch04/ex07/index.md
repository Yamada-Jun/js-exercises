### システムに負荷を与えるコード
下記のコードを実行すると、処理が無限ループに入りプログラムが終了しなくなる
```javascript
set42(";while(1){}	//");
```
### セキュリティの問題となるコード
下記のコードを実行すると、任意の変数の情報(パスワードなど)をコンソール出力させることができてしまう
```javascript
set42("; console.log(password);	//");
```